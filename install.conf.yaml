- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: .zshrc
    ~/.custom_commands.sh: .custom_commands.sh
    ~/.gitignore: .gitignore
    ~/.gitconfig: .gitconfig
    ~/.oh-my-zsh-config.sh: .oh-my-zsh-config.sh
    ~/.ssh/config: .ssh_config

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
